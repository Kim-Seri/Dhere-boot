<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springbootstudy.dhere.mappers.NoticeMapper">

	<insert id="noticeWrite" parameterType="Notice">
		INSERT INTO notice(notice_no, title, content, reg_date, email, file, picture)
		VALUES(#{noticeNo}, #{title}, #{content}, SYSDATE(), #{email}, #{file}, #{picture})
	</insert>

	<select id="getNoticeList" >
		
	</select>

	

</mapper>