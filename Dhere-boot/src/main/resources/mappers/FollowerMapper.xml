<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springstudy.dhere.mappers.FollowerMapper">

    <insert id="insertFollow" parameterType="com.springbootstudy.dhere.domain.Follower">
        INSERT INTO follower (follower_email, following_email)
        VALUES (#{follower_email}, #{following_email})
    </insert>
    
    <delete id="deleteFollow" parameterType="map">
        DELETE FROM follower
        WHERE follower_email = #{followerEmail} 
        AND following_email = #{followingEmail}
    </delete>
    
    <select id="isFollowing" parameterType="map" resultType="int">
	    SELECT COUNT(*)
	    FROM follower
	    WHERE follower_email = #{followerEmail}
	    AND following_email = #{followingEmail}
	</select>


<!-- 
<select id="followerList"  resultMap="followerResultMap">
		SELECT
			f.follower_email AS followerEmail,
			f.following_email AS followingEmail,
			m.email,
			m.picture,
			m.nickname,
			m.job
		FROM follower f
			JOIN member m ON f.following_email = m.email
		WHERE f.follower_email = #{followerEmail}
	</select>
	
	<resultMap id="followerResultMap" type="Follower">
		<id property="email" column="email" />
		<result property="followerEmail" column="followerEmail" />
		<result property="followingEmail" column="followingEmail" />
		<result property="picture" column="picture" />
		<result property="nickname" column="nickname" />
		<result property="job" column="job" />
	</resultMap>

 -->

</mapper>